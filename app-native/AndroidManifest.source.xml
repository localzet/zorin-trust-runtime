<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.zorin.trustruntime"
    android:versionCode="50"
    android:versionName="5.0.0">
    <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="35" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <application android:label="Zorin Trust Runtime" android:hasCode="true">
        <activity android:name="android.app.NativeActivity" android:exported="true">
            <meta-data android:name="android.app.lib_name" android:value="zorin_native_core" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name="dev.zorin.trustruntime.TrustService"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="persistent cryptographic owner-device trust over authenticated USB/ADB transport" />
        </service>
    </application>
</manifest>
